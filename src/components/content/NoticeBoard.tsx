import React from "react";

const NoticeBoard = () => {
  return (
    <section className="rounded-md border-gray-300 border-[1px] row-span-2">
      NoticeBoard
    </section>
  );
};

export default NoticeBoard;
